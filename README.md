# Salary Prediction from Job Descriptions

Дипломный проект по курсу **Data Science** (Нетология, 2024)  
Автор: Андрей Юрков

## 🎯 Цель

Построить модель машинного обучения, которая предсказывает **диапазон годовой зарплаты** специалиста на основе текста вакансии (описания) и данных о компании.

## 📥 Исходные данные

- **Вакансии**: ID, описание, тип занятости (`full-time`), период оплаты (почасово/ежемесячно/ежегодно), `min_salary`, `max_salary`  
- **Компании**: ID, название  
- Все данные приведены к единому годовому формату оплаты (с учётом средней годовой нагрузки в США: 1757 часов)

## ⚙️ Подход

1. **Фильтрация**: оставлены только вакансии с типом занятости `Full-time`.
2. **Преобразование зарплат**: приведение к годовому эквиваленту.
3. **Предобработка текста**:
   - Приведение к нижнему регистру
   - Удаление стоп-слов и пунктуации
   - Лемматизация (с помощью `pymystem3`)
4. **Векторизация**: TF-IDF для описаний вакансий.
5. **Кодирование признаков**: One-Hot Encoding для названий компаний.
6. **Нормализация**: Min-Max Scaling для целевых переменных (`min_salary`, `max_salary`).

## 🤖 Модели

Обучены и протестированы две модели для каждой из целевых переменных:

- **Random Forest Regressor**
- **Gradient Boosting Regressor**

Оценка качества — по метрикам **R²** и **RMSE** на тестовой выборке.

## 📊 Результаты

Модели демонстрируют умеренную предсказательную способность, что подтверждает:  
> **Описание вакансии и название компании содержат полезную информацию для оценки уровня зарплаты**, но требуют дополнительного обогащения признаками для повышения точности.

## 🛠️ Используемые библиотеки

- `pandas`, `numpy`
- `scikit-learn` (`RandomForestRegressor`, `GradientBoostingRegressor`, `TfidfVectorizer`, `MinMaxScaler`)
- `nltk`, `pymystem3`
- `matplotlib`
